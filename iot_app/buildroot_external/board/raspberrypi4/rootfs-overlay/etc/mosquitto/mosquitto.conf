# Listen on every IPv4 interface so the development sender can connect from
# another computer on the same network.
listener 1883 0.0.0.0

# This keeps initial LAN testing simple. Do not use anonymous access in a
# production image because an MQTT client could send Python code to the device.
allow_anonymous true
