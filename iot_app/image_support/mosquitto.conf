# Listen on every IPv4 interface so the sender can connect from another
# computer on the same development network.
listener 1883 0.0.0.0

# Anonymous access keeps initial testing simple. Production images should use
# TLS, per-device credentials, topic ACLs, and signed application packages.
allow_anonymous true
