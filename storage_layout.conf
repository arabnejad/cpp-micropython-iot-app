# Image partition sizes are written in MiB.
#
# The root partition contains Linux and IoT App. It keeps this fixed size after
# the image is flashed. The data partition uses the rest of the SD card.
ROOT_PARTITION_SIZE_MIB ?= 256
