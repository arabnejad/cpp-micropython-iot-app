# Image partition sizes are written in MiB.
#
# The root partition contains Linux, IoT App, and the video libraries. It keeps
# this fixed size after the image is flashed. The data partition uses the rest
# of the SD card.
ROOT_PARTITION_SIZE_MIB ?= 512
